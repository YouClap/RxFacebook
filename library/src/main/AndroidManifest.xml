<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.youclap.rxfacebook">

    <application>

        <provider
                android:authorities="${applicationId}.rxfacebookprovider"
                android:exported="false"
                android:enabled="true"
                android:name=".RxFacebookInitProvider"/>

        <activity android:name=".login.FacebookLoginHandlerActivity"/>

        <activity android:name=".login.MultiplierActivity"/>
    </application>

</manifest>
